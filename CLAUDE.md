# OCR領収書→会計データ変換 エラー修正ガイド

## 問題: "An image in the conversation exceeds the dimension limit for many-image requests (2000px)"

このエラーは、1回の会話に多数の画像を含めると、各画像の最大寸法が2000pxに制限されるために発生します。

## 修正方法: CLAUDE.md（Cowork手順書）に以下のバッチ処理指示を追加

### Step 1: Vision読み取り の修正

**修正前（問題あり）:**
```
全ての未確認画像（約35件）をVisionで一括読み取りする
```

**修正後:**
```
## Vision読み取りルール（重要）

- 画像は **1回につき最大5枚まで** バッチ処理する
- 各バッチの処理が完了してから次のバッチに進む
- 画像が大きい場合（2000px超）は事前にリサイズしてから送信する
- 処理済み画像のリストを管理し、未処理分を次のバッチで処理する

### バッチ処理の手順:
1. 対象フォルダの画像一覧を取得
2. 5枚ずつグループに分ける
3. 各グループを順番にVisionで読み取る
4. 読み取り結果をJSONLファイルに逐次追記する
5. 全グループの処理が完了するまで繰り返す
```

## Coworkの手順書（CLAUDE.md）推奨テンプレート

```markdown
# OCR解析で領収書を会計データ化

## 制約事項（必ず守ること）
- **1回のメッセージに含める画像は最大5枚まで**
- 2000pxを超える画像は長辺1500pxにリサイズしてから処理する
- エラーが発生した場合、バッチサイズを3枚に減らして再試行する

## 進行状況の管理
- 処理済みファイル名を記録し、重複処理を防ぐ
- 各バッチの完了後に中間結果を保存する

## Step 1: Vision読み取り
対象画像を5枚ずつバッチで読み取る。
各バッチ完了後、結果を02_json/receipts_{run_id}.jsonlに追記する。

## Step 2: JSONL生成
バッチ処理で蓄積されたJSONLを統合する。

## Step 3: レビューCSV生成
03_review/review_{run_id}.csv を出力する。

## Step 4: MF仕訳CSV生成
04_mf_import/shiwake_{run_id}.csv を出力する。

## Step 5: レビューHTML再生成
全件を埋め込んだHTMLを生成する。
```

## 即時の回避策

現在エラーで止まっているセッションでは:
1. **新しいセッションを開始** する（現在のセッションは画像がコンテキストに溜まっている）
2. CLAUDE.mdにバッチ処理の指示を追加してから再実行する
3. 最初のバッチ（5枚）から開始し、順次処理する
