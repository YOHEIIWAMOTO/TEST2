# OCR解析で領収書を会計データ化

## 制約事項（必ず守ること）
- **1回のメッセージに含める画像は最大5枚まで**
- 2000pxを超える画像は長辺1500pxにリサイズしてから処理する
- エラーが発生した場合、バッチサイズを3枚に減らして再試行する
- 全画像を一括で読み込もうとしないこと

## 進行状況の管理
- 処理済みファイル名をスクラッチパッドに記録し、重複処理を防ぐ
- 各バッチの完了後に中間結果を保存する

## Step 1: Vision読み取り（バッチ処理）
1. 対象フォルダの画像一覧を取得する
2. **5枚ずつ**グループに分ける
3. 各グループを順番にVisionで読み取る
4. 読み取り結果を `02_json/receipts_{run_id}.jsonl` に逐次追記する
5. 全グループの処理が完了するまで繰り返す

## Step 2: JSONL生成
バッチ処理で蓄積されたJSONLを統合し、`02_json/receipts_{run_id}.jsonl` を完成させる。

## Step 3: レビューCSV生成
`03_review/review_{run_id}.csv` を出力する。

## Step 4: MF仕訳CSV生成
`04_mf_import/shiwake_{run_id}.csv` を出力する。

## Step 5: レビューHTML再生成
全91件を埋め込んだHTMLを生成する。
